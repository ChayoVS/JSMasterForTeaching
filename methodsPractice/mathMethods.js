//Math.random()
// Retorna un número decimal o float en el rango del 0 al 1(no incluido en la salida)

// Math.floor(valor)
// Devuelve el máximo entero menor o igual a un número.

//combinando métodos math
console.log(Math.floor(Math.random() * 10))

// Math.round()
// Retorna el valor de un número redondeado al entero más cercano. 

// Math.ceil()
// devuelve el entero mayor o igual más próximo a un número dado.

// Math.abs()
// retorna el valor absoluto de un número

// Math.max() 
// devuelve el mayor de cero o más números.

// Math.min() 
// devuelve el menor de cero o más números.

// Math.pow(base, exponente)
// devuelve la  base elevada al exponente.

// Math.round()
// Si la porción fraccionaría del número es 0.5 o mayor, el argumento es redondeado al 
// siguiente número entero superior. Si la porción de la fracción del número es menor 
// a 0.5, el argumento es redondeado al siguiente número entero inferior.

// Math.sqrt()
// retorna la raíz cuadrada de un número

// Math.trunc()
// retorna la parte entera de un numero al remover cualquier dígito fraccionario.

